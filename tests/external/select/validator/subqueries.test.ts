/**
 * Subquery Validation Tests (Adapted)
 *
 * The current type-only validator focuses on table/column names in the main query.
 * Subquery/CTE structural validation is intentionally out of scope.
 */

export type SubqueriesValidatorTestsPass = true;
